# Routing

Routes belong inside:

src/routes

Pages belong inside:

src/pages

Protected routes:

ProtectedRoute

Admin routes:

AdminRoute

Public routes:

PublicRoute

App.js should only compose providers, layouts and routes.